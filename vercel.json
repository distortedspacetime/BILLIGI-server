{ "version": 2, 
  "builds": [
    {
      "use": "@vercel/node"
    }
  ],
  "rewrites": [{ "source": "/(.*)", "destination": "/api" }],
  "methods": ["GET", "POST", "PUT", "PATCH", "DELETE", "OPTIONS"]
}